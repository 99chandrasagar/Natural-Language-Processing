fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9
pydantic==2.7.1

# Embeddings (runs locally, no API key needed)
sentence-transformers==3.0.1

# Vector search
faiss-cpu==1.8.0

# LLM providers (install the one you use)
openai>=1.30.0           # for GPT-4o / GPT-3.5
anthropic>=0.28.0        # for Claude
requests>=2.31.0         # for Ollama (local LLaMA/Mistral)

# PDF support (pick one)
pymupdf>=1.24.0          # best quality — pip install pymupdf
# pypdf>=4.2.0           # lighter alternative

# Utilities
python-dotenv==1.0.1     # load .env files
numpy>=1.26.0
